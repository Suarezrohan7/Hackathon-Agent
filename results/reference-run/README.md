# reference-run/

Populated by the author's real `python -m eval.harness --which both` run once an
`ANTHROPIC_API_KEY` is set. Holds `summary.md`, `report.html`, `raw.json`, and the
per-case trajectories, so a reviewer can see the exact expected output before
spending a token — then reproduce it with the same command.

_Not yet generated — pending the eval run._
